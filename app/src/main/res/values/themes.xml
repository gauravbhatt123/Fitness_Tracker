<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base App Theme - Material 3 -->
    <style name="Theme.FitBody" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/secondary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        <item name="colorSecondaryContainer">@color/secondary_container</item>
        <item name="colorOnSecondaryContainer">@color/on_secondary_container</item>
        <item name="colorTertiary">@color/tertiary</item>
        <item name="colorTertiaryContainer">@color/tertiary_container</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/on_error</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/on_surface_variant</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Dark splash/onboarding theme -->
    <style name="Theme.FitBody.Dark" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:colorBackground">@color/onboarding_bg</item>
        <item name="colorSurface">@color/dark_surface</item>
        <item name="android:statusBarColor">@color/status_bar_dark</item>
        <item name="android:navigationBarColor">@color/onboarding_bg</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.FitBody.Splash" parent="Theme.FitBody.Dark">
        <item name="android:windowBackground">@color/onboarding_bg</item>
    </style>

    <!-- Auth screens theme (light, clean) -->
    <style name="Theme.FitBody.Auth" parent="Theme.FitBody">
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- For backward compat references in old manifest -->
    <style name="Theme.Pogo" parent="Theme.FitBody" />
    <style name="signTheme" parent="Theme.FitBody.Auth" />
    <style name="loginTheme" parent="Theme.FitBody.Auth" />

</resources>
